image: gitpod/workspace-dotnet:latest

tasks:
  - name: Restore & Build
    init: |
      dotnet dev-certs https 
      dotnet restore
      dotnet build
  - name: Run
    command: dotnet run

github:
  prebuilds:
    master: true

vscode:
  extensions:
    - muhammad-sammy.csharp
